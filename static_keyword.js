//static - it is a keyword that defines properties and method
//that belongd to the class itself rather than its object

// simple bhasa mein static keyword ka use karenge to 
//bina object create kiye hi hum methods aur 
//properties ko uske class ke naam sse  use kr sakte hain


class robot{
    static boost(){
        console.log("fly")
    }
}
robot.boost();